export class ShortList {
    id!: number;            // Identifiant de la catégorie ou de l'élément
    idUser!: number;        // Identifiant de l'utilisateur (utilisé pour lier l'élément à un utilisateur spécifique)
    idElement!: number;     // Identifiant de l'élément de catégorie
    typeElement!: string;
}
